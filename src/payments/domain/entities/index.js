/**
 * Payments Context – Entidades de dominio.
 */
const Payment = require('./Payment');
const PaymentApplication = require('./PaymentApplication');

module.exports = {
  Payment,
  PaymentApplication,
};
