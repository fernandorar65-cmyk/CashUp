/**
 * IAM / Users Context – Entidades de dominio.
 */
const User = require('./User');
const Role = require('./Role');
const UserRole = require('./UserRole');

module.exports = {
  User,
  Role,
  UserRole,
};
